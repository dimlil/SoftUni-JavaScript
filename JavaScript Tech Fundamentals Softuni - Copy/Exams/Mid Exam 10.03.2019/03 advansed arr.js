function solve(input) {
    // var arr = input.split("\n");
    //process.stdout.write(JSON.stringify(arr));
    // for(let i=0;i<input.length;i++){
        let arr = input.split(",");
        console.log(arr);
    // }
}
solve(1,2,3);