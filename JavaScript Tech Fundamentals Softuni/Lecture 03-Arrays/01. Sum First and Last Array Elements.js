function solve(arr) {
    let sum=Number(arr[0])+Number(arr[arr.length-1]);
    console.log(sum);
    /*
    let first=Number(arr.shift());
    let last=Number(arr.pop());
    */

}
solve(['20','10','20']);