unswift()-добавя елемент в началото на масива
swift()-премахва елемент в началото на масива
push()-добавя елемент в края на масива
pоp()-премахва елемент в края на масива
slice()-реже масива
splice()-добавя/премахва позиция в масива

Мотираща функция- променя масива, но не създава нов масив. Ако има референции към този масив(пр. let arr2[]=arr[])
и те се променят

Мотираща функция- Създава нов масив, който използва стойностите на стария. Ако има референции към този 
масив(пр. let arr2[]=arr[]) те не се променят

slice не мотира
splice-мотира

.filter()- в скобите се пише функция 
пр. let result= arr.filter(e=>true); Връща всеки елемент

let result= arr.filter(e=>e.startsWith('f'));Връща всеки елемент започваш с f
//дълъг варянт
пр. let result= arr.filter(function(element){ 
return element.startsWith('f'); 

let result= arr.filter(e=>!e.startsWith('f'));Връща всеки елемент който нр започва с f

.map()- връща нов масив като го променя
пр.let result= arr.filter(e=>'Mr. '+ e); Ще добави мистар на всеки елемент
пр.let newArr=arr.map(function(element){ 
return element*2;

let result=arr
	.filter(e=>arr.indexOf(e)%2!=0)
	.map(e=>e*2)
	.reverse()
	.join(' ');

-------------------
sorting

console.log(names.sort());