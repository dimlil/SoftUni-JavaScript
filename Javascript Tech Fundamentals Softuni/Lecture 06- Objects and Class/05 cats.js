function solve(arr) {
    class Cats{
        constructor(name,age){
            this.name=name;
            this.age=age;
    }
    meaw(){
        console.log(`${this.name}, age ${this.age} says Meow`);
}
}
let some=arr.map(line=>line.split(" "));
arr.forEach(cat=>{

})
}